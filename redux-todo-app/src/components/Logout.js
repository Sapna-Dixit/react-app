import React from 'react'

const Logout = () =>{
    return(
        <div>
            <h1> Logout <span className="user_name">Smith!</span></h1>
            <button className="logout_btn">Logout</button>
        </div>
    )
}
export default Logout;